import "string"
import "math"

let upper = string_upper("test")
match upper:
    "TEST" => say "string_upper: OK"
    _ => say "string_upper: FAILED"

let sqrt = math_sqrt(16)
match sqrt:
    4 => say "math_sqrt: OK"
    _ => say "math_sqrt: FAILED"
