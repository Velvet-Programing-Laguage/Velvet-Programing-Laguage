import "string"
import "math"
import "random"

set name = "velvet"
say string_upper(name)

set rand = random_int(1, 10)
say "Random: " + rand

do window:
    title "Velvet Demo"
    size 800, 600
    textinput "input" "Type here"
    button "Show" action:
        say "You typed something!"
