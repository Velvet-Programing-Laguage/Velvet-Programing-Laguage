import "math"
import "string"

struct Point:
    x: Number
    y: Number

let p = Point { x: 10, y: 20 }

fn add_point(p):
    let sum = math_add(p.x, p.y)
    return sum

say add_point(p)

do window:
    title "Advanced App"
    size 800, 600
    textinput "name" "Enter name"
    button "Greet" action:
        say string_upper("user")
