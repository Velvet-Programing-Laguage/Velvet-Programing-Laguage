import "math"
import "string"

set num = 25
say "Square root: " + math_sqrt(num)

def greet(name):
    say "Hello, " + string_upper(name)

do window:
    title "My App"
    size 800, 600
    button "Greet" action:
        greet("User")
