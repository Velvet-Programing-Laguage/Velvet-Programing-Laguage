import crypto

# AES encryption
fn crypto_aes_encrypt(data: string, key: string) {
    return crypto.aes_encrypt(data, key)
}

# SHA-3 hashing
fn crypto_sha3(data: string) {
    return crypto.sha3(data)
}
