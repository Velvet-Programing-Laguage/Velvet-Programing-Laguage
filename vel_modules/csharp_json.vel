fn json_parse(json_str) {
    return csharp_json("parse", json_str)
}

fn json_serialize(obj) {
    return csharp_json("serialize", obj)
}

fn json_validate(json_str) {
    return csharp_json("validate", json_str)
}
